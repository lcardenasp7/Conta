[phases.setup]
nixPkgs = ['nodejs-18_x', 'npm-9_x']

[phases.install]
cmds = [
    'npm install --omit=dev',
    'npx prisma generate'
]

[phases.build]
cmds = [
    'echo "Build phase completed"'
]

[start]
cmd = 'npm start'