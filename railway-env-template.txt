# Variables de entorno para Railway
# Copia estas variables en tu Railway Dashboard -> Variables

NODE_ENV=production
JWT_SECRET=villas-san-pablo-super-secret-key-2024-railway-production
JWT_EXPIRES_IN=24h

# Rate limiting para producción
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Configuraciones de seguridad
HELMET_CSP_ENABLED=true
CORS_ORIGIN_STRICT=true

# NOTA: Las siguientes variables las configura Railway automáticamente:
# - DATABASE_URL (cuando agregues el servicio PostgreSQL)
# - PORT (Railway asigna el puerto automáticamente)
# - RAILWAY_PUBLIC_DOMAIN (para FRONTEND_URL)

# Para configurar FRONTEND_URL manualmente (opcional):
# FRONTEND_URL=https://tu-app.railway.app